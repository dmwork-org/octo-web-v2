# a1-no-useeffect-fetch

> ⚠️ **未经陈超确认**。本骨架由 CC 自动生成,`intent` 字段为 `rules.md` 意译,`PROMPT.md` 任务和断言组合待陈超从 pilot 真实场景补。目录编号为 CC 提议方案,未定稿。

> **骨架** — Week 3-4 陈超根据 pilot 项目真实场景补全。
> **验证规则**: `no-useeffect-fetch`([rules.md](../../taste/rules.md#no-useeffect-fetch))

## Intent

给一个"列表页拉数据"场景,CC 应该用 `loader`,不用 `useEffect + fetch`。

## Setup

TODO(week-3-4): 补 fixture(已有 ofetch `~/lib/api.ts` + query key factory)。

## Task

TODO(week-3-4): 补一个真实需求 prompt,覆盖"组件里想拉数据"的高频诱因。

## Expected tanstack_refs

- `router/loader`
- `query/ensureQueryData`
